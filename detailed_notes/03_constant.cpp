#include<iostream>

int main(){
    // constants cannot be changed
    // a constant in a program can't be changed after initialiazation
    // a variable can be a constant if the variable syntax is with const keyword and const keyword is the first thing in the syntax with as usual variable syntax
    // a constant
    // const type name = value;

    const double PI = 3.1416;

    // according to the tradition name of a constant should be in capital letters

    return 0;
}